{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/nettoie-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test netlify/tests/*.test.mjs",
    "audit": "node scripts/audit-seo.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1",
    "marked": "^18.0.11"
  },
  "allowScripts": {
    "esbuild": true
  }
}
